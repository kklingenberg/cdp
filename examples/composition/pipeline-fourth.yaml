---
name: "fourth"
input:
  poll: "${SOURCE}"

steps:
  debug:
    flatmap:
      send-stdout:
        jq-expr: |-
          [
            .[].n, ": ",
            (.[].d | if . == null then "<nothing>" else . end),
            " (",
            (.[].t | map(.p) | join(" --> ")),
            ")"
          ] | join("")
